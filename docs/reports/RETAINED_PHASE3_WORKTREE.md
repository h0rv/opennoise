# Retained phase3 evidence worktree

The merged `data/phase3-public-evidence` worktree remains at
`.worktrees/phase3-public-evidence` pending an explicit archival decision.

At the 2026-09-13 cleanup audit it contained 335 untracked files totaling
2,148,811,523 bytes. Its vaults include 94 distinct content-addressed raw
objects absent from the canonical main checkout, with approximately 1.63 GB
across the vault copies. These are retained as potentially non-reproducible
source evidence. The clean Wikidata worktree and superseded semantic-renderer
worktree were removed separately.

Do not remove this worktree until the unique raw objects and phase3 qualified
artifacts have a selected archival destination and verified inventory.
